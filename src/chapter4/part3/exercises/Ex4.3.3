For each cut with no black edges, we need to choose the minimum-weight crossing edge.
Since all edge weights are unique, we only have once choice of this crossing edge. This holds true every time
we add a new edge to the MST => the MST must be unique