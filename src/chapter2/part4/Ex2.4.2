The idea is wrong because when that maximum item is removed from the stack/queue, we will have to iterate
over every remaining elements to find the maximum again, since stack/queue does not have a self-repair
mechanism like a priority queue.